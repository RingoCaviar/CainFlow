# Render variable video controls from protocols

CainFlow will keep one stable video-node interaction and runtime surface while rendering provider-variable controls and input ports from the selected Declarative protocol configuration and its Protocol variant. This avoids per-protocol node UI branches while retaining one authoritative implementation for prompt entry, task state, recovery, downloading, and result presentation.
